"""
libcal_bookings app URLs.
"""
from django.urls import path  # noqa: F401 imported but never used

from . import views  # noqa: F401 imported but never used

urlpatterns = [
    # path('', views.YourView.as_view(), name='yourview'),
]
